# Ignore everything in .config
#.config/*
#/etc/nixos
#.ssh

# But do not ignore .nix files
#!.config/**/*.nix
#!/etc/nixos/**/*.nix

#Ignore hardware-configuration.nix - it gets generated at install
hardware-configuration.nix
